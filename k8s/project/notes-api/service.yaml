apiVersion: v1
kind: Service
metadata:
  name: notes-api
  namespace: notes
  labels:
    app: notes-api
spec:
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: notes-api
