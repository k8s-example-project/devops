apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: notes
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: https://github.com/k8s-example-project/devops.git
      path: k8s/project/notes-api
      targetRevision: add-k8s-configs
    - repoURL: https://github.com/k8s-example-project/devops.git
      path: k8s/project/notes-ui
      targetRevision: add-k8s-configs
  destination:
    server: https://kubernetes.default.svc
    namespace: notes
  syncPolicy:
    automated:
      selfHeal: true
      prune: true